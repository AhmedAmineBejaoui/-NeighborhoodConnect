{
  "name": "neighborhood-monorepo",
  "private": true,
  "workspaces": ["apps/api", "apps/web"],
  "scripts": {
    "dev": "concurrently \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "npm --prefix apps/api run dev",
    "dev:web": "npm --prefix apps/web run dev",
    "build": "npm --prefix apps/api run build && npm --prefix apps/web run build",
    "start": "npm --prefix apps/api run start && npm --prefix apps/web run start",
    "test": "npm --prefix apps/api run test && npm --prefix apps/web run test"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
